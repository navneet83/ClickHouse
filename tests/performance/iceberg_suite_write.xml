<!-- https://clickhouse.com/docs/getting-started/example-datasets/tpch -->
<!-- Scale factor: 10 -->
<test profile_all_queries="1">
    <settings>
        <allow_insert_into_iceberg>1</allow_insert_into_iceberg>
        <mutations_sync>2</mutations_sync>
        <iceberg_metadata_log_level>none</iceberg_metadata_log_level>
        <iceberg_delete_data_on_drop>1</iceberg_delete_data_on_drop>
    </settings>

    <!-- WRITE suite: the INSERT, mutation and maintenance paths of `IcebergLocal` fed from the CI-attached `tpch10` dataset, one dedicated table per measured query so the randomized query order cannot couple them. -->
    <substitutions>
        <substitution>
            <name>database</name>
            <values>
                <value>iceberg_suite_write</value>
            </values>
        </substitution>
        <substitution>
            <name>table</name>
            <values>
                <value>nation</value>
                <value>region</value>
                <value>part</value>
                <value>supplier</value>
                <value>partsupp</value>
                <value>customer</value>
                <value>orders</value>
                <value>lineitem</value>
            </values>
        </substitution>
        <!-- One commit per value: the seeded tables carry 32 snapshots, manifests and data files before anything is measured. -->
        <substitution>
            <name>seed</name>
            <values>
                <value>0</value>
                <value>1</value>
                <value>2</value>
                <value>3</value>
                <value>4</value>
                <value>5</value>
                <value>6</value>
                <value>7</value>
                <value>8</value>
                <value>9</value>
                <value>10</value>
                <value>11</value>
                <value>12</value>
                <value>13</value>
                <value>14</value>
                <value>15</value>
                <value>16</value>
                <value>17</value>
                <value>18</value>
                <value>19</value>
                <value>20</value>
                <value>21</value>
                <value>22</value>
                <value>23</value>
                <value>24</value>
                <value>25</value>
                <value>26</value>
                <value>27</value>
                <value>28</value>
                <value>29</value>
                <value>30</value>
                <value>31</value>
            </values>
        </substitution>
        <!-- One position-delete commit per value, for the merge-on-read read tax. -->
        <substitution>
            <name>del_n</name>
            <values>
                <value>0</value>
                <value>1</value>
                <value>2</value>
                <value>3</value>
                <value>4</value>
                <value>5</value>
                <value>6</value>
                <value>7</value>
                <value>8</value>
                <value>9</value>
            </values>
        </substitution>
    </substitutions>

    <create_query>CREATE DATABASE IF NOT EXISTS {database}</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.{table}</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.mut_upd</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.mut_upd_part</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.mut_del</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.mut_del_part</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.tf_month</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.tf_bucket</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.tf_truncate</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.tf_identity</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.ins_small_files</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.ins_parallel</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.fmt_orc</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.commit_shallow</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.commit_deep</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.stat_wide</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.stat_narrow</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.nested</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.meta_hint</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.meta_gzip</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.meta_fullpath</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.maint_compact</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.maint_manifest</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.maint_expire</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.maint_orphan</create_query>
    <create_query>DROP TABLE IF EXISTS {database}.mor</create_query>

    <!-- The eight INSERT targets, empty at start; measures are `Float64` and fixed text is `String` because `Iceberg::getIcebergType` maps neither `Decimal` nor `FixedString`. -->

    <create_query>
        CREATE TABLE {database}.nation
        (
            n_nationkey Int32,
            n_name String,
            n_regionkey Int32,
            n_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_nation_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.region
        (
            r_regionkey Int32,
            r_name String,
            r_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_region_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.part
        (
            p_partkey Int32,
            p_name String,
            p_mfgr String,
            p_brand String,
            p_type String,
            p_size Int32,
            p_container String,
            p_retailprice Float64,
            p_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_part_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.supplier
        (
            s_suppkey Int32,
            s_name String,
            s_address String,
            s_nationkey Int32,
            s_phone String,
            s_acctbal Float64,
            s_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_supplier_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.partsupp
        (
            ps_partkey Int32,
            ps_suppkey Int32,
            ps_availqty Int32,
            ps_supplycost Float64,
            ps_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_partsupp_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.customer
        (
            c_custkey Int32,
            c_name String,
            c_address String,
            c_nationkey Int32,
            c_phone String,
            c_acctbal Float64,
            c_mktsegment String,
            c_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_customer_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.orders
        (
            o_orderkey Int32,
            o_custkey Int32,
            o_orderstatus String,
            o_totalprice Float64,
            o_orderdate Date,
            o_orderpriority String,
            o_clerk String,
            o_shippriority Int32,
            o_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_orders_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.lineitem
        (
            l_orderkey Int32,
            l_partkey Int32,
            l_suppkey Int32,
            l_linenumber Int32,
            l_quantity Float64,
            l_extendedprice Float64,
            l_discount Float64,
            l_tax Float64,
            l_returnflag String,
            l_linestatus String,
            l_shipdate Date,
            l_commitdate Date,
            l_receiptdate Date,
            l_shipinstruct String,
            l_shipmode String,
            l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_lineitem_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <!-- Mutation targets: `mut_upd` / `mut_del` unpartitioned so a scattered predicate touches every data file, `mut_upd_part` / `mut_del_part` on the `month` transform so it touches one of 84. -->

    <create_query>
        CREATE TABLE {database}.mut_upd
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_mut_upd_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.mut_upd_part
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_mut_upd_part_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
        PARTITION BY toMonthNumSinceEpoch(l_shipdate)
    </create_query>

    <create_query>
        CREATE TABLE {database}.mut_del
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_mut_del_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.mut_del_part
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_mut_del_part_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
        PARTITION BY toMonthNumSinceEpoch(l_shipdate)
    </create_query>

    <!-- Partition-transform targets: `bucket[8]` and `truncate[250000]` both yield 8 partitions so their delta is the per-row transform, `month` (84) and identity (3) vary the cardinality. -->

    <create_query>
        CREATE TABLE {database}.tf_month
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_tf_month_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
        PARTITION BY toMonthNumSinceEpoch(l_shipdate)
    </create_query>

    <create_query>
        CREATE TABLE {database}.tf_bucket
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_tf_bucket_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
        PARTITION BY icebergBucket(8, l_orderkey)
    </create_query>

    <create_query>
        CREATE TABLE {database}.tf_truncate
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_tf_truncate_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
        PARTITION BY icebergTruncate(250000, l_partkey)
    </create_query>

    <create_query>
        CREATE TABLE {database}.tf_identity
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_tf_identity_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
        PARTITION BY l_returnflag
    </create_query>

    <!-- Small-file, parallel-write and ORC targets, unpartitioned so insert_lineitem is their baseline. -->

    <create_query>
        CREATE TABLE {database}.ins_small_files
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_ins_small_files_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.ins_parallel
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_ins_parallel_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.fmt_orc
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_fmt_orc_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'ORC')
    </create_query>

    <!-- Commit-depth pair: the same tiny insert onto 32 existing snapshots and onto none, isolating the part of a commit that scales with the manifest list. -->

    <create_query>
        CREATE TABLE {database}.commit_shallow
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_commit_shallow_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.commit_deep
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_commit_deep_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <!-- Statistics pair: 8M values as 32 columns and as 1, since `MultipleFileWriter::consume` runs `DataFileStatistics` per column per chunk. -->

    <create_query>
        CREATE TABLE {database}.stat_wide
        (
            c00 Int32, c01 Int32, c02 Int32, c03 Int32, c04 Int32, c05 Int32, c06 Int32, c07 Int32,
            c08 Int32, c09 Int32, c10 Int32, c11 Int32, c12 Int32, c13 Int32, c14 Int32, c15 Int32,
            c16 Int32, c17 Int32, c18 Int32, c19 Int32, c20 Int32, c21 Int32, c22 Int32, c23 Int32,
            c24 Int32, c25 Int32, c26 Int32, c27 Int32, c28 Int32, c29 Int32, c30 Int32, c31 Int32
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_stat_wide_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.stat_narrow
        (
            c00 Int32
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_stat_narrow_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <!-- Nested types: `getIcebergType` maps `Tuple` to struct, `Array` to list and `Map` to map, and TPC-H has no nested column. -->

    <create_query>
        CREATE TABLE {database}.nested
        (
            id Int64,
            arr Array(Int32),
            m Map(String, Int64),
            t Tuple(a Int32, b String)
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_nested_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <!-- Metadata-commit variants; `iceberg_use_version_hint` is a storage setting, the other two are read from the query context in `IcebergMetadata::createInitial` so they are set on the session. -->

    <create_query>
        CREATE TABLE {database}.meta_hint
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_meta_hint_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
        SETTINGS iceberg_use_version_hint = 1
    </create_query>

    <create_query>SET iceberg_metadata_compression_method = 'gzip'</create_query>
    <create_query>
        CREATE TABLE {database}.meta_gzip
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_meta_gzip_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>
    <create_query>SET iceberg_metadata_compression_method = ''</create_query>

    <create_query>SET write_full_path_in_iceberg_metadata = 1</create_query>
    <create_query>
        CREATE TABLE {database}.meta_fullpath
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_meta_fullpath_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>
    <create_query>SET write_full_path_in_iceberg_metadata = 0</create_query>

    <!-- Maintenance targets, each seeded with 32 commits so the command has real work on its first run. -->

    <create_query>
        CREATE TABLE {database}.maint_compact
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_maint_compact_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.maint_manifest
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_maint_manifest_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.maint_expire
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_maint_expire_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <create_query>
        CREATE TABLE {database}.maint_orphan
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_maint_orphan_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <!-- Merge-on-read target: 10 position-delete commits, so the measured scan pays a stable delete-apply tax in `PositionDeleteTransform`. -->

    <create_query>
        CREATE TABLE {database}.mor
        (
            l_orderkey Int32, l_partkey Int32, l_suppkey Int32, l_linenumber Int32,
            l_quantity Float64, l_extendedprice Float64, l_discount Float64, l_tax Float64,
            l_returnflag String, l_linestatus String,
            l_shipdate Date, l_commitdate Date, l_receiptdate Date,
            l_shipinstruct String, l_shipmode String, l_comment String
        )
        ENGINE = IcebergLocal((SELECT value FROM system.server_settings WHERE name = 'user_files_path') || '/iceberg_suite_write_mor_' || toString(toUnixTimestamp64Micro(now64(6))) || '/', 'Parquet')
    </create_query>

    <!-- 2M rows: enough that a copy-on-write rewrite is dominated by Parquet encoding, few enough that it fits the 15 s `max_execution_time` of a measured run. -->
    <fill_query>
        INSERT INTO {database}.mut_upd SELECT * FROM tpch10.lineitem LIMIT 2000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 2000000, min_insert_block_size_bytes = 0
    </fill_query>
    <fill_query>
        INSERT INTO {database}.mut_upd_part SELECT * FROM tpch10.lineitem LIMIT 2000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 2000000, min_insert_block_size_bytes = 0
    </fill_query>
    <fill_query>
        INSERT INTO {database}.mut_del SELECT * FROM tpch10.lineitem LIMIT 2000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 2000000, min_insert_block_size_bytes = 0
    </fill_query>
    <fill_query>
        INSERT INTO {database}.mut_del_part SELECT * FROM tpch10.lineitem LIMIT 2000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 2000000, min_insert_block_size_bytes = 0
    </fill_query>
    <fill_query>
        INSERT INTO {database}.mor SELECT * FROM tpch10.lineitem LIMIT 2000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 2000000, min_insert_block_size_bytes = 0
    </fill_query>

    <!-- Ten setup deletes, one position-delete commit each. -->
    <fill_query>ALTER TABLE {database}.mor DELETE WHERE l_partkey % 200000 = {del_n}</fill_query>

    <!-- Seed commits; `l_partkey` is dense in TPC-H, unlike the sparse `l_orderkey`, so every `seed` value really commits a snapshot. -->
    <fill_query>
        INSERT INTO {database}.commit_deep SELECT * FROM tpch10.lineitem WHERE l_partkey % 32 = {seed} LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0
    </fill_query>
    <fill_query>
        INSERT INTO {database}.maint_compact SELECT * FROM tpch10.lineitem WHERE l_partkey % 32 = {seed} LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0
    </fill_query>
    <fill_query>
        INSERT INTO {database}.maint_manifest SELECT * FROM tpch10.lineitem WHERE l_partkey % 32 = {seed} LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0
    </fill_query>
    <fill_query>
        INSERT INTO {database}.maint_expire SELECT * FROM tpch10.lineitem WHERE l_partkey % 32 = {seed} LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0
    </fill_query>
    <fill_query>
        INSERT INTO {database}.maint_orphan SELECT * FROM tpch10.lineitem WHERE l_partkey % 32 = {seed} LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0
    </fill_query>

    <!-- insert_[table]: one ordered stream, one block, one commit; `max_threads = 1` pins the `LIMIT` row set across servers and the cap keeps `partsupp` / `orders` / `lineitem` inside the 15 s measured-run limit. -->
    <query>
        INSERT INTO {database}.{table} SELECT * FROM tpch10.{table} LIMIT 4000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 4000000, min_insert_block_size_bytes = 0
    </query>

    <!-- upd_every_file: `l_partkey` is dense so `% 10` is exactly one row in ten scattered over every data file, and copy-on-write rewrites the whole table with the same rows every run. -->
    <query>ALTER TABLE {database}.mut_upd UPDATE l_quantity = l_quantity + 1 WHERE l_partkey % 10 = 0</query>

    <!-- upd_one_file: the same mutation confined to one of the 84 monthly partitions, so only that partition's data file is rewritten. -->
    <query>
        ALTER TABLE {database}.mut_upd_part UPDATE l_quantity = l_quantity + 1
        WHERE l_shipdate &gt;= toDate('1995-06-01') AND l_shipdate &lt; toDate('1995-07-01') AND l_partkey % 10 = 0
    </query>

    <!-- del_every_file: `DELETE` is merge-on-read, so the first run writes the delete files and later runs measure planning plus a full scan with them applied. -->
    <query>ALTER TABLE {database}.mut_del DELETE WHERE l_suppkey % 10 = 0</query>

    <!-- del_one_file: the same delete restricted to one monthly partition, so pruning keeps the scan to one data file. -->
    <query>
        ALTER TABLE {database}.mut_del_part DELETE
        WHERE l_shipdate &gt;= toDate('1996-03-01') AND l_shipdate &lt; toDate('1996-04-01') AND l_suppkey % 10 = 0
    </query>

    <!-- tf_*: the same 4M rows as insert_lineitem, partitioned four different ways. -->
    <query>
        INSERT INTO {database}.tf_month SELECT * FROM tpch10.lineitem LIMIT 4000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 4000000, min_insert_block_size_bytes = 0
    </query>
    <query>
        INSERT INTO {database}.tf_bucket SELECT * FROM tpch10.lineitem LIMIT 4000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 4000000, min_insert_block_size_bytes = 0
    </query>
    <query>
        INSERT INTO {database}.tf_truncate SELECT * FROM tpch10.lineitem LIMIT 4000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 4000000, min_insert_block_size_bytes = 0
    </query>
    <query>
        INSERT INTO {database}.tf_identity SELECT * FROM tpch10.lineitem LIMIT 4000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 4000000, min_insert_block_size_bytes = 0
    </query>

    <!-- ins_small_files: 20 data files in one commit; the block size has to come down with the row cap, because the writer only rolls over on a chunk boundary. -->
    <query>
        INSERT INTO {database}.ins_small_files SELECT * FROM tpch10.lineitem LIMIT 2000000
        SETTINGS max_threads = 1, max_insert_threads = 1, max_block_size = 100000,
                 min_insert_block_size_rows = 100000, min_insert_block_size_bytes = 0,
                 iceberg_insert_max_rows_in_data_file = 100000
    </query>

    <!-- ins_parallel: single-threaded source so the row set matches across servers, parallel sink; the data-file count varies, so this one is noisier. -->
    <query>
        INSERT INTO {database}.ins_parallel SELECT * FROM tpch10.lineitem LIMIT 4000000
        SETTINGS max_threads = 1, max_insert_threads = 8,
                 min_insert_block_size_rows = 500000, min_insert_block_size_bytes = 0
    </query>

    <!-- fmt_orc: same rows, same shape, ORC data files instead of Parquet. -->
    <query>
        INSERT INTO {database}.fmt_orc SELECT * FROM tpch10.lineitem LIMIT 4000000
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 4000000, min_insert_block_size_bytes = 0
    </query>

    <!-- commit_shallow / commit_deep: identical 100-row commits onto an empty table and onto 32 snapshots; commit_shallow is also the meta_* baseline. -->
    <query>
        INSERT INTO {database}.commit_shallow SELECT * FROM tpch10.lineitem LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0
    </query>
    <query>
        INSERT INTO {database}.commit_deep SELECT * FROM tpch10.lineitem LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0
    </query>

    <!-- stat_wide / stat_narrow: 8M values either way, 32 columns against 1. -->
    <query>
        INSERT INTO {database}.stat_wide SELECT
            toInt32(number + 0), toInt32(number + 1), toInt32(number + 2), toInt32(number + 3), toInt32(number + 4), toInt32(number + 5), toInt32(number + 6), toInt32(number + 7),
            toInt32(number + 8), toInt32(number + 9), toInt32(number + 10), toInt32(number + 11), toInt32(number + 12), toInt32(number + 13), toInt32(number + 14), toInt32(number + 15),
            toInt32(number + 16), toInt32(number + 17), toInt32(number + 18), toInt32(number + 19), toInt32(number + 20), toInt32(number + 21), toInt32(number + 22), toInt32(number + 23),
            toInt32(number + 24), toInt32(number + 25), toInt32(number + 26), toInt32(number + 27), toInt32(number + 28), toInt32(number + 29), toInt32(number + 30), toInt32(number + 31)
        FROM numbers(250000)
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 250000, min_insert_block_size_bytes = 0
    </query>
    <query>
        INSERT INTO {database}.stat_narrow SELECT toInt32(number) FROM numbers(8000000)
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 8000000, min_insert_block_size_bytes = 0
    </query>

    <!-- ins_nested: struct, list and map columns through the writer. -->
    <query>
        INSERT INTO {database}.nested SELECT
            toInt64(number),
            CAST(range(number % 8), 'Array(Int32)'),
            map('k' || toString(number % 4), toInt64(number)),
            tuple(toInt32(number), toString(number))
        FROM numbers(500000)
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 500000, min_insert_block_size_bytes = 0
    </query>

    <!-- meta_*: the same 100-row commit as commit_shallow, with one metadata knob changed each time. -->
    <query>
        INSERT INTO {database}.meta_hint SELECT * FROM tpch10.lineitem LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0
    </query>
    <query>
        INSERT INTO {database}.meta_gzip SELECT * FROM tpch10.lineitem LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0
    </query>
    <query>
        INSERT INTO {database}.meta_fullpath SELECT * FROM tpch10.lineitem LIMIT 100
        SETTINGS max_threads = 1, max_insert_threads = 1,
                 min_insert_block_size_rows = 100, min_insert_block_size_bytes = 0, write_full_path_in_iceberg_metadata = 1
    </query>

    <!-- maint_compact: rewrites the 32 seeded data files on the first run, then measures planning over an already-compacted table. -->
    <query>OPTIMIZE TABLE {database}.maint_compact SETTINGS allow_experimental_iceberg_compaction = 1</query>

    <!-- maint_manifest: the threshold is lowered so the 32 seeded manifests are above it; once consolidated `compactIcebergManifests` returns early. -->
    <query>
        OPTIMIZE TABLE {database}.maint_manifest MANIFEST
        SETTINGS allow_experimental_iceberg_compaction = 1, iceberg_manifest_min_count_to_compact = 8
    </query>

    <!-- maint_expire: drop all but the newest snapshot; later runs find nothing to expire and return. -->
    <query>
        ALTER TABLE {database}.maint_expire EXECUTE expire_snapshots(retain_last = 1)
        SETTINGS allow_experimental_expire_snapshots = 1
    </query>

    <!-- maint_orphan: constant per run, since with the default three-day threshold nothing is old enough to delete and every run pays the full listing and reachability walk. -->
    <query>
        ALTER TABLE {database}.maint_orphan EXECUTE remove_orphan_files()
        SETTINGS allow_iceberg_remove_orphan_files = 1
    </query>

    <!-- mor_read: full scan of a table carrying 10 position-delete files. -->
    <query>SELECT sum(l_extendedprice), count() FROM {database}.mor</query>

    <drop_query>DROP TABLE IF EXISTS {database}.{table}</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.mut_upd</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.mut_upd_part</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.mut_del</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.mut_del_part</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.tf_month</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.tf_bucket</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.tf_truncate</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.tf_identity</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.ins_small_files</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.ins_parallel</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.fmt_orc</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.commit_shallow</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.commit_deep</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.stat_wide</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.stat_narrow</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.nested</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.meta_hint</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.meta_gzip</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.meta_fullpath</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.maint_compact</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.maint_manifest</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.maint_expire</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.maint_orphan</drop_query>
    <drop_query>DROP TABLE IF EXISTS {database}.mor</drop_query>
</test>
